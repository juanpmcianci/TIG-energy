[package]
name = "tig-algorithms"
version = "0.1.0"
edition = "2021"
description = "TIG Algorithm implementations for Energy Arbitrage"
license = "MIT"

[dependencies]
anyhow = "1.0"
rand = { version = "0.8", features = ["small_rng"] }
tig-challenges = { path = "../tig-challenges" }


